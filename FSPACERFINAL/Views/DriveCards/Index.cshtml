@model IEnumerable<FSPACERFINAL.Models.DriveCard>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Model)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Ratio)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.GearNumber)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ratio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GearNumber)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DriveNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DriveNumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DriveNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
